const leapYears = function(year) {
    let leapYears = True;

    if (year / 4 % 0) {
        return leapYears;
    }

    return leapYears;
};

// Do not edit below this line
module.exports = leapYears;
